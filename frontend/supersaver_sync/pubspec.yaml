name: supersaver_sync
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  bloc: ^8.1.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  coupon_uikit: ^0.2.0
  dropdown_textfield:
  stylish_bottom_bar:
  fl_chart:
  cached_network_image:
  dio: ^4.0.0
  json_annotation:
  json_serializable:
  http:
  blur:
  awesome_card:

dev_dependencies:
  bloc_test: ^9.1.4
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
  build_runner:

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/image/
